<div class="space-y-6">
  <%= render PageHeaderComponent.new(title: "Subscriptions", subtitle: "Track recurring charges") %>

  <%= render CardComponent.new do %>
    <%= render EmptyStateComponent.new(
      title: "Coming Soon",
      description: "Monitor active subscriptions, upcoming billing dates, and total monthly subscription cost.",
      icon: "credit-card"
    ) %>
  <% end %>
</div>
