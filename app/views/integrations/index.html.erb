<div class="space-y-6">
  <%= render PageHeaderComponent.new(title: "Integrations", subtitle: "Connect your services") %>

  <%= render CardComponent.new do %>
    <%= render EmptyStateComponent.new(
      title: "Coming Soon",
      description: "Link bank accounts via Plaid, scan receipts with OCR, and connect external services.",
      icon: "puzzle-piece"
    ) %>
  <% end %>
</div>
