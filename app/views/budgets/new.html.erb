<div class="max-w-2xl">
  <h1 class="text-3xl font-bold text-gray-900 mb-6">New Budget</h1>
  <%= render "form", budget: @budget %>
  <div class="mt-4">
    <%= link_to "Back to budgets", budgets_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
  </div>
</div>
